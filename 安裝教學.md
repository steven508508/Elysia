# 🛡️ E5 保活精靈 · 完整安裝教學

> **v1.8.1** ｜ 全程在瀏覽器完成，**不需要在自己電腦上裝任何東西**。
> 大約 20 分鐘。建議開兩個分頁：GitHub 和 Azure。
>
> 版本之間改了什麼，看 `CHANGELOG.md`。

---

## 開始前的東西

| 需要 | 說明 |
|---|---|
| 一個 GitHub 帳號 | 免費版就夠 |
| 一個 Microsoft 365 E5 開發者訂閱 | 而且你要有**系統管理員**權限 |
| Telegram | 手機或桌面版都行 |
| 解壓縮後的 `e5-keeper` 資料夾 | 我給你的 zip 解開後的內容 |

**七個步驟，順序不能跳**：

```
1. 建 repo 並上傳檔案
2. Azure 註冊應用程式        ← 拿「用戶端 ID」
3. 建 Telegram Bot           ← 拿「bot token」和「chat id」
4. 建 GitHub PAT             ← 拿「github_pat_...」
5. 填 3 個 Secret
6. 授權 E5 帳號              ← 這步會自動產生第 4 個 Secret
7. 驗收
```

> 為什麼授權排在最後：授權時要把「登入代碼」送到你的 Telegram，
> 拿到的 token 要直接寫進 Secret。所以 Telegram 和 PAT 必須先就位。

---

## 步驟 1 · 建 repo 並上傳檔案

### 1-1 建立 repo

前往 **[github.com/new](https://github.com/new)**：

- **Repository name**：隨意，例如 `e5-keeper`
- **Public / Private**：選 **Public**
- **不要**勾 `Add a README file`（保持全空）
- 按 **Create repository**

> **為什麼建議 Public**：GitHub Actions 對公開 repo 免費不限量，
> 私有 repo 每月只有 2000 分鐘，而預設設定會用掉約 11000 分鐘。
> 你的金鑰放在 Secrets 裡是加密的，公開 repo 也讀不到；
> 執行紀錄則已經做過清洗（見最後一節）。
>
> 真的想用私有 repo：先照做完，最後看 README 的「Actions 額度與成本」調兩個參數。

### 1-2 上傳檔案

建好後頁面上會有一行 **uploading an existing file**，點它。

把解壓後 **`e5-keeper` 資料夾「裡面」的所有東西**拖進去
（是裡面的內容，不是資料夾本身），然後按 **Commit changes**。

> ⚠️ **上傳後一定要檢查 `Actions` 分頁有沒有東西。**
> 如果是空的，代表 `.github` 這個隱藏資料夾沒傳上去。改用這招：
> 在 repo 頁面按鍵盤的 **`.`**（句點鍵），會開啟網頁版 VS Code，
> 把資料夾拖進左側檔案列表，然後在左側 Source Control 圖示裡 commit。

### 1-3 打開兩個開關

**開關 A — 啟用 Actions**
點 `Actions` 分頁。如果出現綠色的啟用按鈕就點一下。

**開關 B — 允許 Actions 寫入 repo**
`Settings` → 左側 `Actions` → `General` → 拉到最下面 **Workflow permissions**
→ 選 **Read and write permissions** → **Save**。

### ✅ 檢查點

`Actions` 分頁左側應該看得到這 6 個：

```
🔑 授權帳號（不需要本機環境）
👥 帳號管理
🛡️ E5 保活
🧪 E5 測試模式
🤖 Telegram 指令輪詢
📈 E5 每週統計
```

看不到就是 1-2 沒傳成功，回去用 `.` 那招重來。

---

## 步驟 2 · Azure 註冊應用程式

用你的 **E5 系統管理員帳號**登入 **[portal.azure.com](https://portal.azure.com)**。

### 2-1 新增註冊

搜尋列打 **Microsoft Entra ID**（舊名 Azure AD）進去
→ 左側 **應用程式註冊** → 上方 **＋ 新增註冊**

| 欄位 | 填什麼 |
|---|---|
| 名稱 | 隨意，例如 `E5 Keeper` |
| 支援的帳戶類型 | **任何組織目錄中的帳戶和個人 Microsoft 帳戶** |
| 重新導向 URI | **留空**（裝置碼流程不需要） |

按 **註冊**。

### 2-2 複製用戶端 ID 📋

註冊完會跳到概觀頁，複製 **應用程式 (用戶端) 識別碼**。

長得像 `11111111-2222-3333-4444-555555555555`。
**先貼到記事本**，步驟 6 要用。

### 2-3 開啟公用用戶端流程 ⚠️

左側 **驗證** → 拉到最下面 **進階設定**
→ **允許公用用戶端流程** 選 **是** → 上方 **儲存**

> 🚨 **這步最多人漏掉。** 沒開的話步驟 6 一定失敗，
> 而且錯誤訊息（`unauthorized_client`）看起來跟這件事毫無關係。

### 2-4 加權限

左側 **API 權限** → **＋ 新增權限** → **Microsoft Graph** → **委派的權限**

搜尋框逐一搜尋並勾選這 17 個：

```
offline_access          openid                  profile
User.Read               User.ReadBasic.All      Mail.ReadWrite
Mail.Send               MailboxSettings.Read    Files.ReadWrite.All
Calendars.ReadWrite     Contacts.ReadWrite      Tasks.ReadWrite
Notes.Read              People.Read             Sites.Read.All
Team.ReadBasic.All      Directory.Read.All
```

勾完按 **新增權限**，回到列表後再按 **代表 <你的組織> 授與管理員同意** → **是**。

> 少勾幾個不會壞，對應的 API 只會在通知裡顯示 ⚠️ 403。
> 但 **`offline_access` 絕對不能少** —— 沒有它就拿不到 refresh token，整套用不了。

**另外兩個是選用的**，加了可以讓報告更乾淨（少 3 個 ⚠️）：

```
UserAuthenticationMethod.Read
Reports.Read.All
```

`Reports.Read.All` 讀的是整個租用戶的使用量報表，範圍比較大，而且**除了權限之外
帳號還要有 Entra 管理員角色**（全域管理員或報表讀取者）才生效。
不想擴大權限範圍的話，跳過這兩個完全不影響保活。

### ✅ 檢查點

API 權限列表裡每一列的「狀態」欄都應該是綠色勾勾 **已授與**。

---

## 步驟 3 · 建立 Telegram Bot

### 3-1 生一隻 bot

在 Telegram 搜尋 **[@BotFather](https://t.me/BotFather)**（有藍色驗證勾勾的那個）
→ 點 **START** → 傳 `/newbot`

依指示取兩個名字：

1. **顯示名稱**：隨意，例如 `我的 E5 保活精靈`
2. **使用者名稱**：必須以 `bot` 結尾，例如 `my_e5_keeper_bot`

完成後 BotFather 會給你一串 token 📋：

```
7123456789:AAF-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

**這是 `TELEGRAM_BOT_TOKEN`**，貼到記事本。

### 3-2 主動跟你的 bot 說一句話 ⚠️

點 BotFather 訊息裡的 `t.me/你的bot名稱` 連結，進去按 **START**，隨便傳一句 `hi`。

> 🚨 **這步不能省。** Telegram 規定 bot 不能主動私訊沒跟它說過話的人。
> 沒做的話，之後所有通知都會發不出去。

### 3-3 拿你的 chat id 📋

在 Telegram 搜尋 **[@userinfobot](https://t.me/userinfobot)** → 傳 `/start`，
它會回你一串數字 `Id: 123456789`。

**這是 `TELEGRAM_CHAT_ID`**，貼到記事本。

> **請務必使用「你和 bot 的一對一私人對話」，不要用群組。**
> 群組的 chat id 是負數（`-100...`），那種情況必須額外設定 `TELEGRAM_OWNER_IDS`，
> 否則所有指令都會被拒絕。這是刻意設計的 ——
> 只檢查群組 id 的話，群裡任何人都能對你的 E5 帳號下指令。

---

## 步驟 4 · 建立 GitHub PAT

前往 **[github.com/settings/personal-access-tokens/new](https://github.com/settings/personal-access-tokens/new)**
（路徑：`Settings` → `Developer settings` → `Personal access tokens` → `Fine-grained tokens`）

| 欄位 | 填什麼 |
|---|---|
| Token name | 隨意，例如 `e5-keeper` |
| Expiration | 建議 1 年（到期要重新產生） |
| Repository access | **Only select repositories** → 選你步驟 1 建的 repo |

**Repository permissions** 展開，找到這兩項，**都設成 Read and write**：

| 權限 | 用途 |
|---|---|
| **Contents** | 建立 ✅ Verified commit（顯示為你本人） |
| **Secrets** | 授權結果與輪換後的 token 自動寫入 |

按最下面 **Generate token**，複製那串 `github_pat_...` 📋

**這是 `GH_PAT`**，貼到記事本。

> ⚠️ **這串只會顯示一次**，關掉頁面就看不到了。
>
> 用傳統 token（classic）也可以，勾 `repo` 這個 scope 就同時涵蓋兩者，
> 但它的權限範圍是你**所有的** repo，建議還是用細粒度的。

---

## 步驟 5 · 填 3 個 Secret

回到你的 repo → `Settings` → 左側 `Secrets and variables` → `Actions`
→ **New repository secret**

依序建立這三個（**名稱要一字不差，全大寫**）：

| Name | Secret |
|---|---|
| `TELEGRAM_BOT_TOKEN` | 步驟 3-1 那串 |
| `TELEGRAM_CHAT_ID` | 步驟 3-3 那串數字 |
| `GH_PAT` | 步驟 4 那串 `github_pat_...` |

第四個 `E5_ACCOUNTS` **先不用管** —— 下一步會自動幫你建好。

### ✅ 檢查點

頁面上應該列出 3 個 secret。

---

## 步驟 6 · 授權你的 E5 帳號

這一步會產生 `E5_ACCOUNTS`。

### 6-1 執行授權

`Actions` 分頁 → 左側點 **🔑 授權帳號（不需要本機環境）**
→ 右邊出現 **Run workflow ▾** 按鈕，點它，填：

| 欄位 | 填什麼 |
|---|---|
| **帳號別名** | 例如 `E5-主帳號`（之後通知裡會顯示這個名字） |
| **Azure 應用程式的用戶端 ID** | 步驟 2-2 複製的那串 |
| **預期登入的帳號** | 你的 E5 帳號 email，例如 `admin@xxx.onmicrosoft.com` |
| **租用戶** | 保持 `common` 不動 |
| **同一個帳號已存在時覆寫掉** | 保持勾選 |

按綠色 **Run workflow**。

> 💡 **「預期登入的帳號」請務必填。** 萬一登入代碼被別人搶去用他自己的
> 微軟帳號兌換，系統會發現登入的不是你而中止，不會污染你的設定。

### 6-2 完成登入

十幾秒後，**Telegram** 會收到：

```
🔑 E5 保活精靈 · 帳號授權

1️⃣ 開啟 👉 https://microsoft.com/devicelogin
2️⃣ 輸入代碼 👉 KQPW-3M2X   (點一下可複製)
3️⃣ 用你要保活的 E5 帳號登入並同意授權

⏳ 代碼 15 分鐘內有效，完成後這裡會自動回報結果。
🎯 只接受 ad***@xxx.onmicrosoft.com 登入，其他帳號會被拒絕。
```

照著做：開網址 → 貼代碼 → 用 **E5 帳號**登入 → 同意授權。

> 🔒 代碼**只會出現在 Telegram**，不會出現在 Actions 日誌裡。
> 公開 repo 的日誌是即時串流給所有人看的，代碼印在那裡等於送給別人兌換。

### 6-3 確認結果

回到 Telegram，應該會收到：

```
✅ 已新增帳號：E5-主帳號
🙋 你的名字  <ad***@xxx.onmicrosoft.com>

目前 E5_ACCOUNTS 共有 1 個帳號：
1. E5-主帳號　ad***@xxx.onmicrosoft.com

👉 下一步：到 Actions 跑一次「🧪 E5 測試模式」確認全部正常。
```

### 要加更多帳號？

重跑一次 6-1，換一個別名和對應的 email 就好。同一個 email 會被覆寫。

### ✅ 檢查點

`Settings` → `Secrets and variables` → `Actions` 現在應該有 **4 個** secret。

---

## 步驟 7 · 驗收

`Actions` → 左側 **🧪 E5 測試模式** → **Run workflow**：

| 欄位 | 填什麼 |
|---|---|
| 帳號 | `all` |
| 類別 | `全部` |
| 只驗證 token 與權限 | 不勾 |

按 **Run workflow**。一兩分鐘後 Telegram 會收到完整報告：

```
🧪 E5 保活精靈 · 測試模式
🕐 2026-08-15 21:30:11  Asia/Taipei
🔀 觸發方式：workflow_dispatch
━━━━━━━━━━━━━━━━━━━━━━
✅ E5-主帳號  ad***@xxx.onmicrosoft.com
🔑 委派權限 refresh_token  🔄 token 已輪換
🙋 你的名字
📊 49 ✅　3 ⚠️　0 ❌　（共 52）　⏱️ 1.7s
⏳ 訂閱 DEVELOPERPACK_E5 剩 47 天（2026-10-01）

✅ 200 mail.list              142ms  收到 10 封
✅ 200 files.drive             98ms  已用 1.2 GB / 1024 GB（0.1%）
⚠️ 403 dir.authmethods          1ms  Authorization_RequestDenied
...
```

**收到這個就代表全部裝好了 🎉**

> ⚠️ 是正常的，不用管 —— 那是你沒授與的權限（例如報表類需要系統管理員），
> 或資料本來就不存在（例如你沒設大頭貼）。只有 ❌ 才需要處理。

---

## 裝好之後，它什麼時候自己跑？

| 什麼 | 台北時間 | 頻率 |
|---|---|---|
| 🛡️ **保活** | **09:23–10:13**、**17:47–18:37**、**01:11–02:01** | 每天 3 次 |
| 🤖 收 Telegram 指令 | 全天候 | 每 5 分鐘 |
| 📈 每週統計 | 週一 09:30 | 每週 1 次 |

保活時間是**區間**不是定點：基準點是 09:23 / 17:47 / 01:11，
程式啟動後再隨機等 0–50 分鐘才動手，所以每天實際時刻都不一樣。

三件要知道的事：

1. **GitHub 排程是「盡力而為」**，尖峰時段誤差 5–15 分鐘是正常的。
2. **第一次自動執行要等到下一個時段**才會來 —— 所以步驟 7 的手動測試一定要做。
3. GitHub 會停用「60 天沒 commit」的 repo 排程，但精靈每次執行都會寫紀錄回 repo，
   這點自動免疫。

---

## 日常操作：直接傳訊息給你的 bot

| 指令 | 作用 |
|---|---|
| `/test all` | 立刻跑**全部**帳號的**全部** API |
| `/test E5-主帳號` | 只跑指定帳號 |
| `/test E5-主帳號 mail` | 只測該帳號的郵件類 API |
| `/check all` | 只驗證 token 與權限，不實際呼叫 |
| `/run` | 立刻執行一次正常保活 |
| `/status` | 看最後一次執行結果 |
| `/list` | 列出所有帳號與啟用狀態 |
| `/disable <帳號>` | 暫停該帳號，但保留 token |
| `/enable <帳號>` | 恢復 |
| `/rename <舊> <新>` | 改顯示名稱 |
| `/remove <帳號> confirm` | 移除（不可復原） |
| `/report` | 立刻產生統計報告 |
| `/ping` | 確認精靈還活著 |
| `/help` | 指令說明 |

指令由輪詢收取。GitHub 的排程是「盡力而為」，所以**通常 5 分鐘內、尖峰時可能 15–30 分鐘**才會執行。
覺得太久就直接去 `Actions` 頁面手動跑，效果一樣。

> 💡 **為什麼帳號管理只能靠指令、不能手動改 Secret**：
> GitHub Secret 是寫得進去、讀不回來的。你在網頁上打開 `E5_ACCOUNTS` 看不到內容，
> 想改其中一個帳號就得整包重寫 —— 而 refresh token 你根本讀不出來，
> 等於所有帳號都要重新授權。這些指令是由程式讀出解密後的清單、改完再寫回去，
> 所以能單獨處理一個帳號。
>
> 不想用 Telegram 的話，`Actions` → **👥 帳號管理** 有同樣的功能。

---

## 卡住了？

<details>
<summary><b>Actions 分頁是空的 / 看不到那 5 個 workflow</b></summary>

`.github` 資料夾沒上傳成功。在 repo 頁面按鍵盤 **`.`** 開啟網頁版 VS Code，
把資料夾拖進左側檔案列表，再從左側 Source Control 圖示 commit。
</details>

<details>
<summary><b>授權時出現 unauthorized_client / invalid_client</b></summary>

99% 是**步驟 2-3 沒做**。
回 Azure → 應用程式註冊 → 你的應用 → **驗證** → 最下面
**允許公用用戶端流程**選 **是** → 儲存，再重跑一次授權。
</details>

<details>
<summary><b>Telegram 完全收不到訊息</b></summary>

依序檢查：

1. **有沒有做步驟 3-2？**（主動對 bot 傳過話）沒做的話 bot 不能私訊你
2. `TELEGRAM_CHAT_ID` 有沒有貼錯，或不小心貼成群組的負數 id
3. 到 Actions 看該次執行的日誌，如果有 `Telegram 回應 401` → bot token 貼錯了
4. 如果有 `chat not found` → chat id 貼錯了
</details>

<details>
<summary><b>授權說「缺少 GH_PAT，無法把授權結果寫進 Secret」</b></summary>

`GH_PAT` 這個 Secret 沒建立、或名稱打錯。
必須完全等於 `GH_PAT`（全大寫、中間是底線）。
</details>

<details>
<summary><b>通知裡某個帳號顯示 🚫 取得 token 失敗</b></summary>

通知裡會直接寫原因和修法。最常見的是 `invalid_grant`（refresh token 失效），
重跑一次步驟 6 的授權即可，其他帳號不受影響。
</details>

<details>
<summary><b>commit 沒有 Verified 標記</b></summary>

`GH_PAT` 少了 **Contents** 權限。回步驟 4 補上，或直接重新產生一個 PAT。
不影響功能，只是 commit 少一個綠勾。
</details>

<details>
<summary><b>想確認設定到底哪裡不對</b></summary>

`Actions` → **🧪 E5 測試模式** → Run workflow → 把
**只驗證 token 與權限** 勾起來。它只檢查設定，不會實際呼叫任何 API。
</details>

---

## 這個 repo 公開，別人看得到什麼？

**看得到**：你的 workflow 設定、`config.yml`、以及 `history/` 裡的執行紀錄 ——
只有帳號別名、每次的成功／失敗數、失敗的 API 名稱與狀態碼、token 輪換的時間點。

**看不到**：

- 任何 token、client secret
- email（連遮蔽版都不寫進檔案，因為網域會洩漏你的 Azure 租用戶）
- 訂閱剩餘天數與 SKU 名稱
- 你的姓名、組織名、信箱內容、OneDrive 用量
- 登入代碼、Telegram bot token、chat id、Telegram 使用者名稱

這些細節**只會出現在你的 Telegram 私訊**裡。

想自己驗證的話，repo 裡附了 `tools/selftest_privacy.py` ——
它用一個「會外洩的假帳號」跑完整流程，再逐項檢查公開日誌與提交檔案有沒有漏出東西。

---

## 之後想調整

改 `config.yml` 然後 commit，下次執行就生效。常用的：

| 想做什麼 | 改哪裡 |
|---|---|
| 換執行時段 | `.github/workflows/keepalive.yml` 的 `cron`（**UTC 時間**） |
| 減少隨機等待 | `schedule.random_delay_max_minutes` |
| 每次多跑幾個 API | `run.min_apis` / `run.max_apis` |
| 不要寄信給自己 | `features.send_self_mail: false` |
| 完全不做寫入操作 | `features.write_operations: false` |
| 通知只要摘要 | `notify.detail: summary` |
| 改時區 | `timezone` |

完整參數說明看 `README.md`。
