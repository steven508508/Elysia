# 版本紀錄

## 1.8.1

**長期可運行性** — 讓它幾年後還跑得起來。

先講結論：**pip 那邊本來就沒問題**。鎖死版本不會讓未來裝不起來（PyPI 不會刪已發布的版本，
Python 版本也一併釘在 workflow 裡），反而是防投毒的必要手段。真正會讓「未來跑不了」的是
**GitHub Actions 的 Node runtime 汰換** —— 歷史上 node12 → node16 → node20 每次退場
都讓大量舊 workflow 直接失敗。

- **Actions 從 v4/v5 升到 v6**（`checkout` v6.1.0、`setup-python` v6.3.0）。
  v4/v5 跑在 Node 20 上，遲早會被 GitHub 停用；v6 是 Node 24，可用年限長得多。
  照舊用 commit SHA 釘死，Dependabot 會繼續追更新
- **Python 3.11 → 3.13**。全套測試已在 3.13 上驗證通過
- **相依套件更新到目前最新**（`charset-normalizer` 3.5.1），其餘本來就是最新
- **`git push` 備援不再相依於 checkout 的內部做法**。v6 把認證資訊改存到
  `$RUNNER_TEMP` 而非 `.git/config` —— 我們自備一條用 credential helper
  從環境變數讀 token 的路徑，token 不會出現在指令列也不會寫進工作目錄。
  （主要的簽章提交路徑本來就不碰 git 認證，這只是備援的備援）
- 修掉 `selftest.py` 一個 flaky 斷言：1.8 把訂閱查詢改成 40% 機率後，
  測試仍假設「一定會查到」，靠運氣通過。現在強制必查讓它穩定

## 1.8.0

**擬人化：改行為節奏，不是改內容** — 這一版動的是最容易被辨識的東西。

原本的模式：每天不多不少剛好 3 次、永遠在同樣 3 個時段、API 間隔均勻 1~8 秒、
每次都在同一個位置查訂閱、建立的檔案 0.9 秒後就刪掉、週末跟平日一模一樣。
真人不是這樣用的。

- **每天跑幾次會變**。8 個候選時段隨機挑 2~5 個（週末 1~3 個），
  組合由「當地日期 + repo 名稱」推導 —— 同一天的每一班算出同一份行程，
  但每天不同、每個人也不同（避免全世界跑這套工具的人同時打微軟）。
  沒被選中的班次幾秒內結束，不呼叫任何 API。
  實測一年：平均 3.06 次/天、範圍 1~5、**零活動的日子 0 天**
- **呼叫節奏改成叢發式**。56% 是連續操作（≤2 秒，像開一個畫面連帶觸發的請求）、
  36% 停下來看內容、8% 整個離開一下（1~4 分鐘）。
  中位數 1.8 秒但平均 24 秒 —— 右偏分布，而不是原本那種均勻的機器節奏
- **延後清除**。建立的檔案／草稿／事件留到本輪結束才刪，存活數分鐘而非 0.9 秒。
  「建完立刻刪」是整套流程裡最不像真人的動作。一樣不會累積
- **訂閱查詢改成機率性**（40%）。每次都在固定位置打同一支 API 太規律了；
  一天跑 2~5 次，平均仍會查到 1~2 次，到期提醒不受影響
- **偶爾重複開同一個東西**（12%）—— 真人會重新整理
- 隨機延遲從 0~50 分鐘調成 0~30 分鐘（時段變多了，不需要那麼大的窗口）

全部可在 `config.yml` 的 `humanize` 區塊調整，`enabled: false` 就完全退回舊行為。
新增 `tools/selftest_humanize.py` 驗證上述所有統計性質。

⚠️ 誠實說明：微軟的續訂判準沒有公開，沒有人能保證這些有用。合理推測納入評估的是
API 呼叫的量、種類與**時間分布** —— 時間分布正是這一版在改善的，所以它比改檔案內容
更值得做。但仍然是推測，不是保證。

## 1.7.0

**降低寫入操作的機器指紋** — 內容與命名改成中性隨機。

先說清楚：沒有證據顯示微軟會讀檔案內容來判斷你是不是真的在用，
所以這一版對「續訂機率」的實際幫助我判斷接近零。做它的理由是原本的痕跡
實在太好認 —— 成本很低，那就不要留。

- 🟠 **雲端硬碟裡會留下一個永久的 `E5Keeper` 資料夾**。上傳測試檔時會自動建立它，
  而程式只刪檔案不刪資料夾。改成直接放根目錄，不再產生任何殘留資料夾
  （**既有的那個要自己手動刪**）
- 檔案內容從固定的 52 bytes 樣板，改成隨機長度（約 150–1100 bytes）的中性文字
- 檔名從 `e5keeper-20260815-025157-tm9x2k.txt` 改成 `notes-2026-08-15-x19q.md`
  這類，名稱、日期格式、副檔名都隨機
- 郵件主旨、行事曆事件、待辦清單、聯絡人的名稱全部改用中性字樣，
  不再帶 `[E5Keeper]` 前綴

清除失敗時，通知裡仍會附上確切名稱，方便你手動清理。

## 1.6.0

**指令通道可靠性強化** — 補掉四個會讓指令靜悄悄失效的洞。

- 🔴 **指令可能永久消失**。原本是「先推進位移，再執行指令」，job 若在執行途中
  被逾時砍掉或 runner 異常，那條指令就再也不會被處理，而且你不會知道。
  改成**跑完才推進位移**；為了避免某條指令每次都弄死 job 造成無限重試，
  執行前會先記下嘗試次數，同一條試滿 3 次就跳過並通知你
- 🟠 **帳號設定壞掉會連指令通道一起死**。指令通道正是你用來發現東西壞掉的管道，
  它不能有這種相依。現在 `E5_ACCOUNTS` 空的或格式壞掉時，輪詢仍會運作，
  `/help`、`/list` 照樣回得出來，並主動告訴你哪裡有問題
- 🟡 **指令通道死掉沒人知道**。排程被 GitHub 停用、或輪詢每次都失敗時，
  你只會覺得「指令怎麼沒反應」。保活執行時（一天 3 次）會查一次輪詢 workflow
  的狀態，超過 2 小時沒跑、或最後一次失敗，就在通知裡警告
- 🟡 **等待時間不透明**。收到 `/test`、`/run` 時會告訴你這條指令等了多久才被撿到，
  讓你分得出「還在排隊」和「根本沒收到」

新增 `tools/selftest_reliability.py` 驗證上述行為。

## 1.5.0

**帳號管理** — 新增 `e5keeper/accounts.py` 與 **👥 帳號管理** workflow。

GitHub Secret 是寫得進去、讀不回來的：你在網頁上打開 `E5_ACCOUNTS` 看不到內容，
想改其中一個帳號就得整包重寫，而 refresh token 讀不出來，等於所有帳號都要重新授權。
現在由程式讀出解密後的清單、改完再寫回去，就能單獨處理一個帳號。

- Telegram：`/list`、`/disable`、`/enable`、`/rename`、`/remove`
- 帳號可用**別名、email 或序號**指定
- `/remove` 與「停用最後一個啟用中的帳號」需要加 `confirm`
- 改名撞到既有別名會被拒絕；沒有 `GH_PAT` 時拒絕操作而非假裝成功
- 帳號被移除到零也不會崩潰，會正常回報「沒有啟用中的帳號」

另外把 `UserAuthenticationMethod.Read` 與 `Reports.Read.All` 加進預設權限清單
（選用，Azure 沒授與的話對應 API 顯示 ⚠️ 403，不影響保活）。

## 1.4.0

**公開紀錄減量** — commit 進 repo 的內容再縮一輪。

- `history/` 與 `STATUS.md` 不再寫入 email（連遮蔽版都不寫 —— 網域完整可見，
  光靠網域就查得出 Azure 租用戶 ID）、訂閱剩餘天數、SKU 名稱
- 新增 `features.history_detail`（`minimal` / `full`）
- 公開場所的 email 一律整個換成 `<email>`，不再是部分遮蔽

## 1.3.0

**公開 repo 安全強化** — 五個真實風險，全部修掉。

- 🔴 **裝置碼曾印進 Actions 日誌**。公開 repo 的日誌是即時串流的，
  有人盯著就能在 15 分鐘內用自己的微軟帳號兌換掉，並把他的帳號寫進你的設定。
  改成只送 Telegram，並新增「預期登入帳號」比對
- 🟠 **完整 email 進了永久 commit**。Graph 的錯誤訊息會原封不動回吐 email，
  AADSTS 會夾帶租用戶 ID。寫入公開檔案前一律清洗
- 🟠 **陌生人可灌爆 repo**。知道 bot 名稱的人每 5 分鐘傳一則就能製造 288 個
  掛在你名下的 commit。改成只有真的有指令要執行時才提交，並加上寄件者身分檢查
- 🟡 **個資直播**。API 摘要含姓名、組織名、信件數、OneDrive 用量。
  明細改為只送 Telegram，Actions 日誌只留狀態碼
- 🟡 **供應鏈**。Actions 改用 commit SHA 釘死、`requirements.txt` 鎖定完整相依樹，
  附 Dependabot 照樣收更新 PR

新增 `tools/selftest_privacy.py` 驗證上述防護。

## 1.2.0

**雲端授權** — 新增 **🔑 授權帳號** workflow，整個安裝流程都在瀏覽器完成，
不需要本機 Python。裝置碼送 Telegram，token 直接加密寫進 Secret。

## 1.1.0

**Verified commit** — 改用 GitHub GraphQL 的 `createCommitOnBranch`，
commit 由 GitHub 自動簽章、顯示 ✅ Verified，不需要管理任何 GPG／SSH 金鑰。

- 身分優先用 `GH_PAT`（顯示為你本人、計入貢獻圖），
  失敗自動退回 `GITHUB_TOKEN`（顯示為 bot，仍然 Verified），
  再失敗才退回一般 `git push`
- 併發衝突時重新同步遠端再套用，不會蓋掉別人剛寫進去的紀錄

新增 `tools/selftest_commit.py` 驗證四條路徑。

## 1.0.0

首版。

- 每天 3 次保活，時間隨機化（基準點 + 0~50 分鐘隨機延遲）
- 52 個 Microsoft Graph API，四大類，含寫入型操作（建立後自動清除）
- 每次執行推 Telegram：摘要 + 逐項 API 明細（狀態碼、耗時、回傳摘要）
- 測試模式：三種觸發方式，跑指定帳號的全部 API、零延遲、逐項回報
- 多帳號、雙認證模式（委派 / 應用程式）、串行執行
- refresh token 輪換後自動加密回寫 Secret
- 訂閱到期倒數提醒、每週成功率統計、執行歷史寫回 repo
- `tools/selftest.py` 離線模擬整套流程
